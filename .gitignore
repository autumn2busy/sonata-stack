node_modules/
dist/
.env
*.js.map

# AIDesigner
.aidesigner/
.mcp.json.aidesigner-backup

# scratch / debugging files (root-scoped so src/ and scripts/ paths are unaffected)
/local.stderr
/local.stdout
/local-*.log
/req.json
/smoke-test.mjs
/test-*.mjs

# one-off ops scripts — read secrets from env only, still gitignored so nothing
# slips in via `git add .`. Only the shell scripts (ci-dist-guard.sh etc.)
# are intended to be tracked under scripts/.
/scripts/*.mjs
!/scripts/seed-test-lead.mjs
